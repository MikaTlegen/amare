import type { Metadata } from "next";
import { StubPage } from "@/components/pages/StubPage";

export const metadata: Metadata = { title: "Лицензия" };

export default function Page() {
  return <StubPage />;
}
