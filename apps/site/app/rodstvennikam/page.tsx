import type { Metadata } from "next";
import { StubPage } from "@/components/pages/StubPage";

export const metadata: Metadata = { title: "Родственникам" };

export default function Page() {
  return <StubPage />;
}
