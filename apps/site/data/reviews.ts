import { CLINIC } from '@/lib/clinic'

export interface Review {
  id: string
  /** Имя как его оставил автор. Фамилию целиком не публикуем. */
  author: string
  /** Роль: сам пациент или родственник — читателю это важно. */
  relation: string
  rating: number
  date: string
  text: string
}

/**
 * Отзывы.
 *
 * ВНИМАНИЕ, читать перед публикацией.
 *
 * Тексты ниже — ПЛЕЙСХОЛДЕРЫ. Я не стал копировать отзывы с 2ГИС
 * по двум причинам:
 *
 * 1. Отзыв принадлежит автору. Перенос чужого текста на коммерческий
 *    сайт без разрешения — нарушение, а в медицине ещё и репутационный
 *    риск: человек может потребовать убрать упоминание своего лечения.
 * 2. Придумывать отзывы за клинику нельзя вообще никогда.
 *
 * Правильных путей два:
 *   — официальный виджет отзывов 2ГИС (тянет реальные отзывы с их
 *     стороны, ничего копировать не нужно, ответственность на 2ГИС);
 *   — собственные отзывы, собранные клиникой с письменного согласия.
 *
 * Рейтинг и число оценок — цифры проверяемые и публичные, их оставляем.
 */
export const REVIEWS: Review[] = [
  {
    id: 'r-1',
    author: '[Имя]',
    relation: 'дочь пациента',
    rating: 5,
    date: '[дата]',
    text: '[ТЕКСТ ОТЗЫВА — взять из 2ГИС через официальный виджет или собрать с согласия автора]',
  },
  {
    id: 'r-2',
    author: '[Имя]',
    relation: 'пациент',
    rating: 5,
    date: '[дата]',
    text: '[ТЕКСТ ОТЗЫВА]',
  },
  {
    id: 'r-3',
    author: '[Имя]',
    relation: 'сын пациента',
    rating: 5,
    date: '[дата]',
    text: '[ТЕКСТ ОТЗЫВА]',
  },
]

export const REVIEWS_SUMMARY = {
  rating: CLINIC.rating.value,
  count: CLINIC.rating.reviews,
  source: CLINIC.rating.source,
  sourceUrl: CLINIC.address.mapUrl,
}
