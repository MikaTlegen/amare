/**
 * Телефон клиники для кабинета опекуна («Позвонить в клинику»).
 *
 * Care — отдельное приложение от apps/site и не может импортировать его
 * lib/clinic.ts. Значение продублировано намеренно; при смене номера
 * поправить в обоих местах, пока не появится общий источник контактов.
 */
export const CLINIC_PHONE = {
  label: '+7 700 525 25 77',
  href: 'tel:+77005252577',
} as const
