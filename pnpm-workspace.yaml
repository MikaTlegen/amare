packages:
  - apps/*
  - packages/*

# Не ставить релизы моложе суток (минуты) — защита от подменённых пакетов. Не ослаблять
minimumReleaseAge: 1440

# pnpm по умолчанию блокирует install-скрипты; разрешаем только нужным пакетам
allowBuilds:
  '@swc/core': true
